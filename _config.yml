title: "Tor Exit Router Page"
description: "Alternative design to the default Tor exit router page."
baseurl: "/tor-exit-page"
plugins:
  - jekyll-redirect-from
exclude:
  - Gemfile
  - redirects.json
